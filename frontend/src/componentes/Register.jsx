import { useRef, useState, useEffect } from "react"


function Register() {
    const userRef = useRef();
    const errRef = useRef();

    return (
        <div>

        </div>
    );
}

export default Register;